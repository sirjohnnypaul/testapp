export default class Product {
    
    constructor(name,number,description,images) {
        this.name = name;
        this.number = number;
        this.description = description;
        this.images = images;
    }
}
